package routines;

import org.apache.commons.codec.binary.Base64 ;


public class encode {

  
        public static String encodeMethod(String content) {
        	
        	return new String(Base64.encodeBase64(content.getBytes()));
        
    }
}
